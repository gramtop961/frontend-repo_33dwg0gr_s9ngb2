export default function Footer() {
  return (
    <footer className="w-full max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pb-10">
      <div className="mt-6 text-xs text-slate-500">
        Use this matrix to facilitate weekly check-ins. Click cells to mark tasks complete (✓) or incomplete (✗). Colors reflect status: green for done, red for pending. Headers and labels use a blue/teal palette, and table cells sit on a subtle light gray background for clarity.
      </div>
    </footer>
  );
}
